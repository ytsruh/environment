# The syntax is "key = value". The whitespace around the equals doesn't matter.
font-size = 16
theme = tokyonight
# add keybinding for quick terminal to ctrl + ` (backtick)
keybind = global:ctrl+grave_accent=toggle_quick_terminal
